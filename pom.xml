<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>uk.ngs</groupId>
    <artifactId>CertWizard</artifactId>
    <version>2.2.0</version>
    <dependencies>
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcprov-jdk18on</artifactId>
            <version>1.84</version>
        </dependency>
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcpkix-jdk18on</artifactId>
            <version>1.84</version>
        </dependency>
        <dependency>
            <groupId>org.restlet.jse</groupId>
            <artifactId>org.restlet</artifactId>
            <version>3.0-M1</version>
        </dependency>
        <dependency>
            <groupId>org.restlet.jse</groupId>
            <artifactId>org.restlet.ext.xml</artifactId>
            <version>3.0-M1</version>
        </dependency>
        <dependency>
            <groupId>org.restlet.jse</groupId>
            <artifactId>org.restlet.ext.httpclient</artifactId>
            <version>3.0-M1</version>
        </dependency> 
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.2</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-core</artifactId>
            <version>2.26.0</version>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-api</artifactId>
            <version>2.26.0</version>
        </dependency>
        <dependency>
            <groupId>commons-validator</groupId>
            <artifactId>commons-validator</artifactId>
            <version>1.10.1</version>
            <type>jar</type>
        </dependency>
        <dependency>
            <groupId>org.swinglabs</groupId>
            <artifactId>swing-layout</artifactId>
            <version>1.0.3</version>
        </dependency>
        <dependency>
            <groupId>com.vdurmont</groupId>
            <artifactId>semver4j</artifactId>
            <version>3.1.0</version>
        </dependency>
        <dependency>
            <groupId>commons-logging</groupId>
            <artifactId>commons-logging</artifactId>
            <version>1.2</version>
        </dependency>
        <dependency>
            <groupId>commons-collections</groupId>
            <artifactId>commons-collections</artifactId>
            <version>3.2.2</version>
        </dependency>
        <dependency>
            <groupId>commons-digester</groupId>
            <artifactId>commons-digester</artifactId>
            <version>2.1</version>
        </dependency>
    </dependencies>
    <repositories>
        <repository>
            <id>maven-restlet</id>
            <name>Public online Restlet repository</name>
            <url>https://maven.restlet.talend.com</url>
        </repository>
        <repository>
            <id>local-repo</id>
            <url>file://${basedir}/lib</url>
        </repository>
    </repositories>
    <packaging>jar</packaging>
    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>25</maven.compiler.source>
        <maven.compiler.target>25</maven.compiler.target>
        <maven.compiler.release>25</maven.compiler.release>
        <mainClass>uk.ngs.certwizard.gui.CertWizardMain</mainClass>
    </properties>

    <licenses>
        <license>
            <name>GPL-2.0</name>
            <!--Use Github link to replace old link-->
            <url>https://github.com/github/choosealicense.com/blob/gh-pages/_licenses/gpl-2.0.txt</url>
        </license>
    </licenses>

    <build>
        <finalName>${project.artifactId}</finalName>
    </build>
    
    <profiles>
        <profile>
            <id>jar-with-dependencies</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <build>
                <resources>
                    <resource>
                        <directory>src/main/java</directory>
                        <includes>
                            <include>**/*.gif</include>
                            <include>**/*.jpg</include>
                            <include>**/*.jpeg</include>
                            <include>**/*.png</include>
                            <include>**/*.properties</include>
                            <include>**/*.xml</include>
                        </includes>
                    </resource>
                    <resource>
                        <directory>src/main/resources</directory>
                    </resource>
                    <resource>
                        <directory>src/test/resources</directory>
                    </resource>
                </resources>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-compiler-plugin</artifactId>
                        <version>3.11.0</version>
                        <configuration>
                            <showDeprecation>true</showDeprecation>
                        </configuration>
                    </plugin>

                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <configuration>
                            <archive>
                                <manifest>
                                    <mainClass>${mainClass}</mainClass>
                                </manifest>
                            </archive>
                            <descriptorRefs>
                                <descriptorRef>jar-with-dependencies</descriptorRef>
                            </descriptorRefs>
                        </configuration>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>single</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    
                    <plugin>
                        <groupId>io.github.fvarrui</groupId>
                        <artifactId>javapackager</artifactId>
                        <version>1.7.6</version>
                        <configuration>
                            <mainClass>${mainClass}</mainClass>
                            <!-- Global fallback, overridden individually below if needed -->
                            <bundleJre>true</bundleJre>
                            <customizedJre>false</customizedJre>
                            <version>2.2.0</version>
                            <displayName>CertWizard</displayName>
                            <organizationName>STFC</organizationName>
                            <organizationUrl>https://stfc.ukri.org</organizationUrl>
                            <organizationEmail>support@grid-support.ac.uk</organizationEmail>
                            <name>CertWizard</name>
                            <licenseFile>${project.basedir}/LICENSE.txt</licenseFile>
                        </configuration>
                        <executions>
                            <execution>
                                <id>bundling-for-windows</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>package</goal>
                                </goals>
                                <configuration>
                                    <platform>windows</platform>
                                    <createZipball>true</createZipball>
                                    <winConfig>
                                        <generateSetup>true</generateSetup>
                                        <generateMsi>true</generateMsi>
                                        <generateMsm>false</generateMsm>
                                        <originalFilename>CertWizard.exe</originalFilename>
                                        <setupMode>askTheUser</setupMode>
                                        <createDesktopIconTask>true</createDesktopIconTask>
                                        <icoFile>src/main/resources/ngs-icon.ico</icoFile>
                                        <removeOldLibs>true</removeOldLibs>
                                    </winConfig>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!--
            macOS packaging uses the JDK's own jpackage instead of fvarrui/javapackager.
            javapackager's mac launcher (universalJavaApplicationStub) is a shell script,
            not a Mach-O binary, and rcodesign can't sign a bundle whose CFBundleExecutable
            isn't Mach-O ("Invalid magic number" errors). jpackage emits a real compiled
            launcher, which both codesign and rcodesign can sign correctly. This profile
            is only auto-activated when running on a Mac (jpackage cannot cross-build mac
            app images from another OS anyway).
        -->
        <profile>
            <id>package-mac</id>
            <activation>
                <os>
                    <family>mac</family>
                </os>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-resources-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>stage-jpackage-input</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <configuration>
                                    <outputDirectory>${project.build.directory}/jpackage-input</outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>${project.build.directory}</directory>
                                            <includes>
                                                <include>${project.build.finalName}-jar-with-dependencies.jar</include>
                                            </includes>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>3.2.0</version>
                        <executions>
                            <execution>
                                <id>jpackage-mac-app-image</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>${java.home}/bin/jpackage</executable>
                                    <arguments>
                                        <argument>--type</argument>
                                        <argument>app-image</argument>
                                        <argument>--name</argument>
                                        <argument>CertWizard</argument>
                                        <argument>--input</argument>
                                        <argument>${project.build.directory}/jpackage-input</argument>
                                        <argument>--main-jar</argument>
                                        <argument>${project.build.finalName}-jar-with-dependencies.jar</argument>
                                        <argument>--main-class</argument>
                                        <argument>${mainClass}</argument>
                                        <argument>--dest</argument>
                                        <argument>${project.build.directory}</argument>
                                        <argument>--app-version</argument>
                                        <argument>${project.version}</argument>
                                        <argument>--vendor</argument>
                                        <argument>STFC</argument>
                                        <argument>--copyright</argument>
                                        <argument>STFC</argument>
                                        <argument>--icon</argument>
                                        <argument>${project.basedir}/src/main/resources/ngs-icon.icns</argument>
                                        <argument>--mac-package-identifier</argument>
                                        <argument>uk.ngs.certwizard</argument>
                                        <argument>--mac-package-name</argument>
                                        <argument>CertWizard</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                            <execution>
                                <!--
                                    jpackage lays out the embedded JDK runtime the same way Oracle/OpenJDK
                                    macOS distributions do: Contents/runtime/Contents/MacOS/libjli.dylib is
                                    a symlink to Contents/runtime/Contents/Home/lib/libjli.dylib, purely so
                                    the runtime folder has a CFBundleExecutable and looks like a bundle.
                                    rcodesign signs the real file once at its Home/lib path, then signs it
                                    again through the symlink as the runtime bundle's "main executable" -
                                    the same physical bytes get mutated after the first pass's bundle
                                    manifest already hashed them, and Apple's notary service rejects it
                                    with "The signature of the binary is invalid." for that path.
                                    Replacing the symlink with an independent real copy before signing
                                    means each path is its own file, signed once, self-consistent - no
                                    change needed to the signing step itself.
                                -->
                                <id>fix-runtime-libjli-symlink</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>/bin/sh</executable>
                                    <arguments>
                                        <argument>-c</argument>
                                        <argument>
                                            set -eu;
                                            LINK="${project.build.directory}/CertWizard.app/Contents/runtime/Contents/MacOS/libjli.dylib";
                                            REAL="${project.build.directory}/CertWizard.app/Contents/runtime/Contents/Home/lib/libjli.dylib";
                                            if [ -L "$LINK" ]; then
                                                rm "$LINK";
                                                cp "$REAL" "$LINK";
                                            fi
                                        </argument>
                                    </arguments>
                                </configuration>
                            </execution>
                            <execution>
                                <id>zip-mac-app</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>ditto</executable>
                                    <arguments>
                                        <argument>-c</argument>
                                        <argument>-k</argument>
                                        <argument>--keepParent</argument>
                                        <argument>${project.build.directory}/CertWizard.app</argument>
                                        <argument>${project.build.directory}/CertWizard-mac.zip</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>